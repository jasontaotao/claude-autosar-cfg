# v1.20.0 MINOR — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close 4 deferred items (v1.18.0 §11.1 + v1.6.0 C2.4) in a single MINOR release. **4 items + 1 housekeeping + 1 release = 6 commits.**

**Architecture:**

- **T1 (B-3)** pure refactor: extract `emitExternDecl`/`emitConstDecl`/`emitLoaderEntry` from `ecuc.ts` → `emit/strategy.ts`; register as Handlebars helpers. Snapshot tests byte-identical.
- **T2 (symlink defense)** security: new `isPathInsideReal` async helper; 3 IPC handlers switch from `isPathInside` → `isPathInsideReal`.
- **T3 (feature-flag async)** refactor + caller audit: `arxml-stream/feature-flag.ts` + `stencil/feature-flag.ts` sync → async. Cache → Promise cache. All callers `await`.
- **T4 (C2.4 GUI applyMutation)** behavioral rewrite: `useScriptStore.applyMutation` routes through `applyPatchSteps` (same engine as CLI). Warnings threaded back to `runResult.warnings`.
- **T5 housekeeping** MEMORY.md rotation + any untracked doc commits.
- **T6 release** package.json 1.19.0 → 1.20.0 + CHANGELOG + `docs/release-notes/v1.20.0/README.md` + commit + tag + push + GH release.

**Tech Stack:** TypeScript 5.x (ESM, `"type": "module"`), Handlebars 4.x, Electron 30, Node ≥22.13, Vitest 1.6, Vite 5, ESLint 8, Prettier. **No new top-level deps.**

**Ship pattern:** single branch `feature/v1-20-0-minor`, 6 commits (T1-T6), squash merge to `main`, tag `v1.20.0`, GitHub Release. Pre-ship joint review (5-agent + verifier) per `claude-autosarcfg-v1-13-x-joint-review` methodology. Ship via `gh api` per `git-push-network-workaround` pattern (proxy `127.0.0.1:7897` intermittent).

---

## Global Constraints

- **Test coverage floor**: 80% per `rules/ecc/common/testing.md` (current baseline 2593 + 2 SKIP / 0 fail).
- **No new `console.log`**: per `rules/ecc/common/coding-style.md`.
- **No `any`**: per TypeScript coding style; use `unknown` then narrow.
- **No mutation**: immutability via spread; reference types stay `readonly`.
- **Comments**: 业务 / 用户面向 用中文；API / 协议 / 字段 用英文。Per user `CLAUDE.md` § 代码注释.
- **Backwards-compatible types**: existing exports may gain async wrappers; sync versions preserved when callers depend on them.
- **Snapshot discipline**: every Cfg.h / Cfg.c / PBcfg.c delta is intentional + regenerated via the test capture script. **T1 snapshot must be byte-identical** (zero drift acceptable).
- **TDD per task**: RED → GREEN → IMPROVE per `rules/ecc/common/testing.md`.
- **Frequent commits**: one commit per task; squash merge at ship time.
- **Code review**: `code-reviewer` agent run after each task per `CLAUDE.md` § 改完代码自动审. CRITICAL/HIGH fixed before next task.
- **Ship workflow**: see `git-push-network-workaround` — `github.com:443` blocked, `api.github.com` 通; use `gh api` for tree/commit/ref/PR/release POSTs.

---

## File Structure (deltas only)

### Modify

- `src/core/generator/modules/ecuc.ts` — remove private `emitExternDecl` / `emitConstDecl` / `emitLoaderEntry` (lines 159-182); import from `emit/strategy.js`
- `src/core/generator/handlebars-helpers.ts` — register 3 new helpers (`externDecl`, `constDecl`, `loaderEntry`)
- `src/shared/paths/isPathInside.ts` — unchanged (kept as sync string-only fallback)
- `src/main/ipc/register.ts` — 2 call sites (lines 258, 276) → `await isPathInsideReal`
- `src/main/ipc/projectWriteArxmlBatchHandler.ts` — 1 call site (line 66) → `await isPathInsideReal`
- `src/main/ipc/bswmdDeleteHandler.ts` — 1 call site (line 36) → `await isPathInsideReal`
- `src/main/arxml-stream/feature-flag.ts` — sync → async throughout
- `src/main/stencil/feature-flag.ts` — sync → async throughout
- `src/renderer/store/useScriptStore.ts` — `applyMutation` rewrite (lines 313-440)
- `src/renderer/store/slices/mutationSlice.ts` — possible adjustments if `applyPatchSteps` interaction requires
- `package.json` — version 1.19.0 → 1.20.0
- `CHANGELOG.md` — add v1.20.0 entry

### Create

- `src/core/generator/emit/strategy.ts` — `emitExternDecl`, `emitConstDecl`, `emitLoaderEntry` (new public surface)
- `src/core/generator/__tests__/emit-strategy.test.ts` — T1 unit tests
- `src/shared/paths/isPathInsideReal.ts` — T2 async helper
- `src/shared/paths/__tests__/isPathInsideReal.test.ts` — T2 symlink fixture tests
- `src/renderer/store/helpers/scriptMutationToPatchStep.ts` — T4 mapping function
- `src/renderer/store/helpers/__tests__/scriptMutationToPatchStep.test.ts` — T4 unit tests
- `src/main/arxml-stream/__tests__/feature-flag-async.test.ts` — T3 async behavior tests (or extend existing)
- `src/main/stencil/__tests__/stencil-flag-async.test.ts` — T3 async behavior tests
- `docs/release-notes/v1.20.0/README.md` — T6 release notes

---

## Quality Gates (run before each commit)

```bash
cd /d/claude_proj2/claude-AutosarCfg
pnpm format:check
pnpm lint
pnpm type-check
pnpm test -- --reporter=dot
pnpm verify
```

Expected: 0 lint warnings, 0 type errors, all existing tests still pass + new tests for this task pass.

---

## Task 1 (T1) — B-3 emit\*Decl extraction

**Spec section**: §2 (`emit*Decl + Handlebars`)
**Files**:
- NEW `src/core/generator/emit/strategy.ts`
- MOD `src/core/generator/modules/ecuc.ts` (remove lines 159-182, add import, replace 3 call sites at lines 257, 260, 264)
- MOD `src/core/generator/handlebars-helpers.ts` (add 3 helper registrations)
- NEW `src/core/generator/__tests__/emit-strategy.test.ts` (6 cases)

**Step 1.1** RED — write `emit-strategy.test.ts` with the 6 expected cases (externDecl / constDecl / loaderEntry × happy + edge).

**Step 1.2** RED — run test, expect "function not found" failures.

**Step 1.3** GREEN — create `src/core/generator/emit/strategy.ts` with the 3 functions verbatim from `ecuc.ts:162-182`. Export them.

**Step 1.4** GREEN — update `ecuc.ts`:
- Remove private functions (lines 159-182).
- Add `import { emitExternDecl, emitConstDecl, emitLoaderEntry } from '../emit/strategy.js';`
- Replace call sites (lines 257, 260, 264) with the imported names. No other code changes.

**Step 1.5** GREEN — register 3 Handlebars helpers in `handlebars-helpers.ts`:

```ts
import { emitExternDecl, emitConstDecl, emitLoaderEntry } from './emit/strategy.js';

export function registerDeclHelpers(): void {
  Handlebars.registerHelper('externDecl', (ident: string, cType: string) =>
    emitExternDecl(ident, cType),
  );
  Handlebars.registerHelper('constDecl', (ident: string, cType: string, init: string) =>
    emitConstDecl(ident, cType, init),
  );
  Handlebars.registerHelper('loaderEntry', (ident: string, cType: string, value: unknown, offset: number) =>
    emitLoaderEntry(ident, cType, value, offset),
  );
}
```

Verify: helper function is called from existing `registerHandlebarsHelpers` setup.

**Step 1.6** IMPROVE — run `pnpm test src/core/generator/__tests__/ecuc.snapshot.test.ts` — snapshots must be byte-identical. If any drift, fix in T1 (do not defer).

**Step 1.7** — run full `pnpm verify` 8-stage. Commit: `refactor(generator): extract emit*Decl helpers to strategy.ts (v1.20.0 T1 B-3)`.

**Acceptance**: 6 new tests pass, all 2593 existing tests still pass, snapshot tests byte-identical, 0 lint / 0 type errors.

---

## Task 2 (T2) — `isPathInsideReal` symlink defense

**Spec section**: §3
**Files**:
- NEW `src/shared/paths/isPathInsideReal.ts`
- NEW `src/shared/paths/__tests__/isPathInsideReal.test.ts` (6 cases using `os.tmpdir()` + `fs.symlink`)
- MOD `src/main/ipc/register.ts` (2 sites at lines 258, 276)
- MOD `src/main/ipc/projectWriteArxmlBatchHandler.ts` (1 site at line 66)
- MOD `src/main/ipc/bswmdDeleteHandler.ts` (1 site at line 36)

**Step 2.1** RED — write `isPathInsideReal.test.ts`:
- happy path: `real-real` → true
- symlink child escapes parent (e.g. `dir/link → /tmp/elsewhere/file`) → false
- symlink parent points to different tree (e.g. `dir → /tmp/elsewhere`, child = `/tmp/elsewhere/file`) → true (still inside real-parent)
- non-existent child → falls back to string compare
- non-existent parent → falls back to string compare
- nested symlink chain (2 levels deep)

**Step 2.2** RED — run test, expect "function not found" failures.

**Step 2.3** GREEN — create `src/shared/paths/isPathInsideReal.ts` per spec §3.1. Use `realpath` from `node:fs/promises`. Fallback to `isPathInside(child, parent)` if realpath fails.

**Step 2.4** GREEN — update 4 IPC call sites:
- `import { isPathInsideReal } from '../../shared/paths/isPathInsideReal.js';`
- Replace `!isPathInside(resolved, manifestDir)` with `!(await isPathInsideReal(resolved, manifestDir))`

**Step 2.5** IMPROVE — run `pnpm test src/main/ipc/__tests__/` — all existing IPC tests must still pass (none of them use symlinks so behavior is unchanged for them).

**Step 2.6** — run full `pnpm verify` 8-stage. Commit: `feat(security): isPathInsideReal async symlink defense (v1.20.0 T2)`.

**Acceptance**: 6 new tests pass, all 2593 + T1 existing tests still pass, 0 lint / 0 type errors.

---

## Task 3 (T3) — Feature-flag readers async migration

**Spec section**: §4
**Files**:
- MOD `src/main/arxml-stream/feature-flag.ts` (sync → async throughout)
- MOD `src/main/stencil/feature-flag.ts` (sync → async throughout)
- MOD all call sites in IPC handlers / router

**Step 3.1** RED — write characterization tests first. Run `pnpm test src/main/arxml-stream/__tests__/feature-flag.test.ts` to confirm green baseline. These tests will be updated to `await` in Step 3.4.

**Step 3.2** GREEN — convert `src/main/arxml-stream/feature-flag.ts`:
- Replace `existsSync` / `readFileSync` with `access` / `readFile` from `node:fs/promises`.
- `loadFromSettingsFile(): Promise<FlagsConfig>`
- `cached: FlagsConfig | null` → `cached: Promise<FlagsConfig> | null`
- `isStreamingEnabled(): Promise<boolean>`
- `isIndexedDbEnabled(): Promise<boolean>`
- `readFlags(): Promise<Result<...>>`
- `setFlagForTest` / `_setSettingsPathForTest` / `_resetFlagCache` signatures unchanged.

**Step 3.3** GREEN — convert `src/main/stencil/feature-flag.ts`:
- Same pattern. `isStencilWizardEnabled(): Promise<boolean>`.

**Step 3.4** GREEN — update callers:
- `git grep -n "isStreamingEnabled\|isIndexedDbEnabled\|isStencilWizardEnabled"` exhaustive pass.
- `src/main/arxml-stream/router.ts` — `await` the result
- `src/main/ipc/swsValidateStub.ts` — `await` the result
- `src/main/ipc/stencilHandler.ts` — `await` the result
- Tests in `src/main/arxml-stream/__tests__/feature-flag.test.ts` — wrap calls in `await`
- Tests in `src/main/stencil/__tests__/` — same

**Step 3.5** IMPROVE — run all flag-related tests. Verify no callers missed.

**Step 3.6** — run full `pnpm verify` 8-stage. Commit: `refactor(feature-flag): async migration of settings.json readers (v1.20.0 T3)`.

**Acceptance**: All existing flag tests pass (with `await`), 0 lint / 0 type errors. Renderer-side `featureFlagsHandler.ts` unchanged (already async).

---

## Task 4 (T4) — GUI `applyMutation` parity

**Spec section**: §5
**Files**:
- NEW `src/renderer/store/helpers/scriptMutationToPatchStep.ts`
- NEW `src/renderer/store/helpers/__tests__/scriptMutationToPatchStep.test.ts` (4 cases)
- MOD `src/renderer/store/useScriptStore.ts` — `applyMutation` rewrite

**Step 4.1** RED — write `scriptMutationToPatchStep.test.ts`:
- set-param maps to `{ op: 'set-param', containerPath, paramName, value }`
- add-child maps to `{ op: 'add-child', parentPath, shortName }`
- remove-child maps to `{ op: 'remove-with-cascade', containerPath, cascade: false }`
- invalid kind (exhaustiveness check) — use `never` type assertion

**Step 4.2** RED — run test, expect "function not found" failures.

**Step 4.3** GREEN — create `scriptMutationToPatchStep.ts` per spec §5.3.

**Step 4.4** RED — characterization tests. Add 6 tests to `useScriptStore.test.ts` for the new `applyPatchSteps` flow:
- happy path: 3 set-param mutations → doc updated + warnings=[] + runState='applied' + IPC `projectSave` called once
- warnings threaded: patch step that emits C8 variant-downgrade warning → runResult.warnings populated
- errors: path-not-found → runResult.errorMessage set + runState='failed' + mutations cleared
- cascade-dialog preserved: remove-child on container with refs → cascade short-circuit detected (may need hybrid path per spec §5.4)
- IPC save failure: `projectSave` returns `{kind: 'write-failed'}` → runResult.errorMessage set + runState='failed'
- no active doc → stub contract preserved (mutations=[], dirty=false)

**Step 4.5** GREEN — rewrite `useScriptStore.applyMutation` per spec §5.2. Use the `scriptMutationToPatchStep` mapper + `applyPatchSteps` + `serializeArxml` + `window.autosarApi.projectSave`.

**Step 4.6** IMPROVE — for cascade-dialog behavior: verify `applyPatchSteps` either exposes a cascade short-circuit (then detect via result.warnings/errors) OR fall back to direct `useArxmlStore.deleteContainer` call for `remove-child` only. Decision documented in commit message.

**Step 4.7** — run `pnpm test src/renderer/store/__tests__/useScriptStore.test.ts`. All 30+ existing tests + 6 new must pass.

**Step 4.8** — run full `pnpm verify` 8-stage. Commit: `refactor(script-engine): useScriptStore.applyMutation routes through applyPatchSteps (v1.20.0 T4 C2.4 GUI parity)`.

**Acceptance**: 4 + 6 = 10 new tests pass, all 2593 + T1+T2+T3 existing tests still pass, 0 lint / 0 type errors.

---

## Task 5 (T5) — Housekeeping

**Files**:
- `MEMORY.md` rotation if test count exceeds threshold
- any untracked docs (`.claude/projects/.../memory/`, `docs/superpowers/...`)
- `package.json` `version` field NOT yet bumped (T6 does that)

**Step 5.1** — `git status --porcelain` to find untracked docs. Stage + commit per file.

**Step 5.2** — if `MEMORY.md` size > 50KB or test count delta > +50, rotate. Otherwise skip.

**Step 5.3** — commit: `chore(docs): v1.20.0 T5 housekeeping`.

---

## Task 6 (T6) — Release

**Files**:
- `package.json` — `version: "1.19.0"` → `"1.20.0"`
- `CHANGELOG.md` — add v1.20.0 entry (top of file)
- `docs/release-notes/v1.20.0/README.md` — per template

**Step 6.1** — bump `package.json` version.

**Step 6.2** — write CHANGELOG entry:

```markdown
## v1.20.0 (2026-07-01) — MINOR

**Generator emit-strategy extraction + IPC symlink defense + feature-flag async migration + GUI applyMutation parity**

See [release notes](docs/release-notes/v1.20.0/README.md) for detail.

### Highlights
- **B-3 emit*Decl extraction**: `emitExternDecl` / `emitConstDecl` / `emitLoaderEntry` moved from `ecuc.ts` (private) to `emit/strategy.ts` (public). Handlebars helpers registered. Zero snapshot drift.
- **`isPathInsideReal` symlink defense**: new async helper used by 3 IPC handlers. Defense-in-depth against attacker-controlled symlinks inside the project directory.
- **Feature-flag readers async migration**: `arxml-stream/feature-flag.ts` + `stencil/feature-flag.ts` sync → async. Cache → Promise cache.
- **C2.4 GUI applyMutation parity**: `useScriptStore.applyMutation` now routes through the same `applyPatchSteps` engine as the CLI. Warnings threaded back into `runResult.warnings`.
```

**Step 6.3** — write `docs/release-notes/v1.20.0/README.md` per template (mirror v1.19.0 README structure).

**Step 6.4** — `pnpm verify` full pipeline.

**Step 6.5** — commit: `chore(release): v1.20.0 MINOR release artifacts`.

**Step 6.6** — push to remote, squash merge if needed, tag `v1.20.0`, GH release via `gh api`.

---

## Cross-Task Checklist

- [ ] All 4 items in scope are closed (T1-T4)
- [ ] Housekeeping complete (T5)
- [ ] Release artifacts + tag + GH release (T6)
- [ ] `pnpm verify` 8-stage GREEN before each commit
- [ ] Code-reviewer dispatched after each task (per `CLAUDE.md`)
- [ ] CRITICAL / HIGH findings fixed before next task
- [ ] No new `console.log` (per `common/coding-style.md`)
- [ ] Comments: 业务 用户面向 中文 / API 协议 字段 英文
- [ ] MEMORY.md PKM capture after ship (per `vault-pkm` skill)

---

## Out of Scope (v1.20.0 explicit non-goals)

- ❌ Handlebars template reshape (`*.hbs` uses helpers directly) → v1.21.0 MINOR
- ❌ Real validator rules → not in this scope
- ❌ Watch mode → not in this scope
- ❌ `arxml-stream` SAX parser swap → not in this scope
- ❌ Multi-doc manifest for headless CLI → not in this scope
- ❌ `useSwsValidatorStore.run()` body wire-up → not in this scope

---

## Risk Register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| B-3 snapshot drift | M | H | Regenerate snapshots in dedicated step; compare bytewise before commit |
| `isPathInsideReal` realpath perf cost | L | M | realpath is cached by Node fs; benchmark before ship |
| Async ripple in feature-flag callers | M | M | `git grep` exhaustive pass before commit |
| C2.4 cascade-dialog regression | H | H | Characterization tests first; hybrid path (remove-child direct, others via applyPatchSteps) if needed |
| ExactOptionalPropertyTypes drift on async wrappers | M | M | Explicit `undefined` discipline in helper return types |

---

## References

- `docs/superpowers/specs/2026-07-01-v1-20-0-minor-design.md` — full design spec
- `docs/superpowers/specs/2026-06-30-v1-18-0-minor-design.md` §11.1 — deferred item source list
- `docs/superpowers/archive/plans/release-notes-v1.6.0.md` line 123 — C2.4 carry-over
- `rules/ecc/common/testing.md` — TDD pattern
- `rules/ecc/common/coding-style.md` — immutability + no mutation
- `git-push-network-workaround.md` — `gh api` ship pattern