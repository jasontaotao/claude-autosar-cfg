# claude-AutosarCfg v1.14.2 PATCH-H — Implementation Plan

**Date**: 2026-06-26
**Spec**: `docs/superpowers/specs/2026-06-26-v1-14-2-patch-design.md`
**Branch**: `feature/v1-14-2-patch` (worktree at `D:/claude_proj2/claude-AutosarCfg-v1-14-2-patch`)
**Parent**: `main@9e3dd27` (v1.14.1 + housekeeping)

## Overview

Close 4 v1.14.1 deferred items in 7 commits. Each item TDD: RED → GREEN → IMPROVE.

## Task Breakdown

### H1 — BSW-SEC-003 wire-up (commits 1+2)

**Anchor files**: `bswmd.ts:889-906`, `diagnostics.ts:30`, `_shared.ts:138-163`, `pipeline.ts`

**Task H1.1** — Parser keeps `''` for empty SHORT-NAME (RED → GREEN)

- RED: `bswmd.test.ts` add case "empty `<STD-INCLUDE>` SHORT-NAME is kept in `includes[]` as `''`"
- GREEN: `buildEcucModule` (line 889-906) — change `flatMap` to return `['']` instead of `[]`; remove the string warning push
- IMPROVE: update existing bswmd.test.ts cases if any assert on the dropped entry

**Task H1.2** — Validator pushes BSW-SEC-003 (RED → GREEN → IMPROVE)

- RED: `validate-module-header-paths.test.ts` add case "pushes BSW-SEC-003 for `inc === ''`"
- GREEN: `validateModuleHeaderPaths` (line 138-163) — add branch `if (inc === '') { push BSW-SEC-003; continue; }` before the existing `validateHeaderPath` check
- IMPROVE: add comment block citing v1.14.1 spec line 163-164

**Task H1.3** — Strict-mode upgrade wired (RED → GREEN)

- RED: `pipeline.test.ts` add case "`strict: true` promotes BSW-SEC-003 from WARN to ERROR"
- GREEN: `pipeline.ts` S5 promotion loop — add `BSW-SEC-003` to the upgrade set
- IMPROVE: confirm the existing S5 promotion is keyed by code (not by enum), so the new addition is a 1-line check

### H2 — STD-INCLUDES emission (commits 3+4)

**Anchor files**: `ecuc.ts:295`, `mcu.ts:215`, `_shared.ts` (new helper), `testdata/generator/*-bswmd.arxml`

**Task H2.1** — `buildSelfIncludes` helper (RED → GREEN)

- RED: `build-self-includes.test.ts` (new file) — 3 cases:
  - empty `selfIncludes` returns `[]`
  - single non-empty entry returns `[entry]`
  - duplicate against `existing` Set is deduped
- GREEN: `_shared.ts` add `buildSelfIncludes` (5-line helper, mirrors `buildReferenceIncludes` immutability pattern)
- IMPROVE: JSDoc citing v1.14.1 spec line 286-288 ("defensive for future BSWMD-supplied includes")

**Task H2.2** — EcuC wire-up (RED → GREEN → IMPROVE)

- RED: `refs-emit.test.ts` add case "H2 self-include + cross-ref to same path → single `#include`"
- GREEN: `ecuc.ts:294-298` — call `buildSelfIncludes(ctx.bswmdIndex.get(eDef.shortName)?.includes, refIncludes)` before `buildReferenceIncludes`; concat the results into the includes array passed to the template
- IMPROVE: verify the dedup case (self + ref same path → one `#include`)

**Task H2.3** — Mcu wire-up (RED → GREEN)

- RED: `mcu.test.ts` add case "H2 self-include appears in Mcu_Cfg.h"
- GREEN: `mcu.ts:214-218` — same shape as H2.2
- IMPROVE: verify cross-generator consistency

**Task H2.4** — BSWMD fixtures + snapshot regen

- `testdata/generator/ecuc-bswmd.arxml` — add `<STD-INCLUDES>` block with 1-2 entries (e.g., `Std/Std_Types.h` + `Os/Os_Cfg.h`)
- `testdata/generator/mcu-bswmd.arxml` — add `<STD-INCLUDES>` block with 1-2 entries
- Regenerate `testdata/generator/ecuc-expected/{PreCompile,Mixed,Refs}-1/EcuC_Cfg.h`:
  ```bash
  RUN_SNAPSHOT_CAPTURE=1 pnpm vitest run src/core/generator/__tests__/ecuc.snapshot.capture.test.ts
  ```
- Verify with `git diff testdata/generator/ecuc-expected/` that:
  - `Refs-1/EcuC_Cfg.h` gains `#include` lines (expected change)
  - `PreCompile-1/EcuC_Cfg.h` and `Mixed-1/EcuC_Cfg.h` are no-op (flat fixtures)

### H3 — EcuC ancestry parity (commit 5)

**Anchor files**: `ecuc.ts:241`, `ecuc.test.ts`, snapshot regen

**Task H3.1** — Switch to `walkContainersWithAncestry` (RED → GREEN)

- RED: `ecuc.test.ts` add 2 cases:
  - nested container emits param with full ancestry path (e.g., `EcuC/PartitionConfig/PartitionBuffer/BufferLength`)
  - leaf-only container emits same as before (no regression)
- GREEN: `ecuc.ts:241-262` — replace `walkContainers(...)` with `walkContainersWithAncestry(...)`; change `const path = ${eDef.shortName}/${cDef.shortName}/${pDef.shortName}` to `const path = ${ancestry}/${pDef.shortName}`
- IMPROVE: update the v1.14.1 comment block to mark "v1.14.2 H3: parity with Mcu — DONE"

**Task H3.2** — Snapshot regen (verify no flat fixture change)

- Same `RUN_SNAPSHOT_CAPTURE=1` command as H2.4
- Verify `git diff` shows no change to flat fixtures (PreCompile, Mixed, Refs all 1-level)

### H4 — capture test bswmdIndex parity (commit 6)

**Anchor files**: `ecuc.snapshot.capture.test.ts:53-65`

**Task H4.1** — Populate capture bswmdIndex (RED → GREEN)

- RED: add a new test case in `ecuc.snapshot.test.ts` that asserts the runtime snapshot test's `bswmdIndex` matches the capture test's expected `bswmdIndex` (locks in the parity)
- GREEN: `ecuc.snapshot.capture.test.ts:59` — replace `bswmdIndex: new Map<string, unknown>()` with the populated EcuC/Os Map
- IMPROVE: extract a shared `makeBswmdIndex()` helper if both files want the same shape

### Release (commit 7)

**Task R.1** — Version + changelog

- `package.json`: `1.14.1` → `1.14.2`
- `CHANGELOG.md`: add v1.14.2 entry listing H1-H4

**Task R.2** — Final pre-ship verification

- All quality gates pass
- `pnpm test -- --reporter=dot` shows 2440+ tests / 0 fail / 0 todo
- Joint review 0C/0H

## Quality Gates (per commit)

```bash
cd /d/claude_proj2/claude-AutosarCfg-v1-14-2-patch
pnpm format:check
pnpm lint
pnpm type-check
pnpm test -- --reporter=dot
```

## Pre-Ship Joint Review

- 2 agents in parallel:
  - `senior`: H3 ancestry path collision with cross-ref `paramByPath` lookups; H2 dedup correctness across EcuC+Mcu; H1 strict-mode upgrade path
  - `security`: H1 BSW-SEC-003 strict-mode upgrade; H2 emit-path security when `modDef.includes` is BSWMD-supplied
- Verdict target: 0C/0H. Fix CRITICAL/HIGH before squash.

## Ship

- Branch: `feature/v1-14-2-patch` (7 commits, may end up squashed at ship time)
- Squash merge → `main`
- Tag `v1.14.2` + GitHub Release (1-paragraph body per Open Question 1)
- Push path: try `git push` first; fallback `gh api` per [[git-push-network-workaround]] if proxy blocks
- Update memory: write `claude-autosarcfg-v1-14-2-shipped.md` + add pointer to `MEMORY.md`

## Self-Review

- [x] Plan is the minimal path from `9e3dd27` to ship
- [x] No "TBD" / placeholder; every task concrete
- [x] Each task is independently committable (TDD-friendly)
- [x] Quality gates run after every commit (not just before ship)
- [x] Joint review is the last gate before ship
- [x] Risks match the spec's risk table; no new risks introduced
- [x] Test count delta matches spec: +8 unit + 1-3 snapshot regen

## Open Questions (from spec, deferred to ship time)

1. GitHub Release body format: 1-paragraph (recommended) vs detailed task list
2. Memory file location: `claude-autosarcfg-v1-14-2-shipped.md` (matches prior 6 ships)
3. H3 snapshot regen scope: all 3 fixtures (recommended) vs only Refs-1
