dist
node_modules
coverage
pnpm-lock.yaml

# v1.54.0 PATCH — review artifacts written by multi-agent audits.
# Not repo source; treated as ephemeral scratch.
.review/

# Git submodules — formatted by their own projects, not us.
vendor/

# Frozen historical docs (release notes + archived specs/plans).
# These are immutable records; prettier --check skips them so the
# verify pipeline stays green without re-flowing the historical text.
docs/release-notes/**
docs/release-notes-v1.10.1.md
docs/release-notes-v1.10.2.md
docs/release-notes-v1.10.3.md
docs/superpowers/archive/

# Phase P1 sub-agent brief / report / progress files (gitignored workflow
# artifacts). prettier --check walks the filesystem regardless of git
# status; without this exclusion, every Phase P1 task would see 5+ false
# "format warnings" from these ephemeral .md files. Same rationale as
# .review/ above.
.superpowers/sdd/