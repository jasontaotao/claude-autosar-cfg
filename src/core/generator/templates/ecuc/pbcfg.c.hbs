{{> license}}
#include "{{moduleHeader}}"

{{#each loaderEntries}}
{{loaderEntry this}}
{{/each}}

void {{moduleShortName}}_PBcfg_Init(void* baseAddr) {
{{#each loaderCalls}}
    {{{this}}}
{{/each}}
}
